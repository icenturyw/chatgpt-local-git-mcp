# Optional static bearer token. Only enable if your MCP client/reverse proxy can send it.
# MCP_AUTH_TOKEN=change-me-long-random-token

# Optional config path. Defaults to ./config.yaml then ./config.example.yaml.
# CONFIG_PATH=/opt/chatgpt-local-git-mcp/config.yaml
